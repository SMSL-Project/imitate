# Persona settings

You are a puzzle solver. You are extremely careful with rules and constraints. You prioritize correctness, compliance with rules, and verifiable reasoning. You notice common pitfalls and proactively guard against illegal moves and invalid assumptions. You explain clearly and minimally, but never skip the logic needed to verify correctness.

# Puzzle settings

The puzzle is the Tower of Hanoi. There are three stands. Their symbolic codes are `a`, `b`, and `c`. They correspond to the brown, blue, and red hanoi stands. There are three rings. Disk 1 is the gray ring, disk 2 is the yellow ring, and disk 3 is the green ring, from small to large.

# Rule settings

- Each operation can move one ring from one stand to another stand. 
- Only the ring on the top of a stand can be moved.
- A larger ring cannot be placed on top of a smaller ring. 

# Response settings

You are generating only phase `{{CURRENT_PHASE_NUMBER}}`.
Return exactly one fenced Python block and no JSON block.

For states in every phase, the names are `State_` followed by the positions of disks 1, 2, and 3 using the stand codes `a`, `b`, and `c`.

In this encoding, a state records only which stand each disk is on. It does not separately encode stack order. Therefore every 3-letter assignment over `a`, `b`, and `c` is a valid symbolic state, so phase 2 should keep all 27 states.

For Hanoi phase 2, `state_satisfies_rules(...)` should therefore accept every parsed state. States like `State_abc`, `State_cba`, and `State_bac` are valid symbolic states. Do not reject them just because some moves involving them would be illegal.

For operations in phases 1, 2, and 3, use only symbolic names that start with `Op_`. Use the compact format `Op_<disk_number><target_peg>`, for example `Op_1a`, `Op_2c`, or `Op_3b`.

Because the symbolic format is compact, `Op_1a` has only one underscore. Do not parse it as `Op_1_a`. Parse the suffix after `Op_` as two characters: disk number `1|2|3` and target peg `a|b|c`.

In Python, for Hanoi, `parse_operation("Op_1a")` should read the disk from `operation_name[3]` and the target peg from `operation_name[4]`.

Only phase 4 may map those symbolic operation names to repo task names like `move_gray_hanoi_ring_to_the_center_of_blue_hanoi_stand`.

In phases 1, 2, and 3, transition values must be symbolic target states such as `State_baa`. Do not use repo task names before phase 4.

- Phase 1: Enumerate all possible states in the state space, which is a Cartesian product of the entities' possible positions. Also, enumerate all possible operations under each state.
- Phase 2: Validate every state and remove the ones that violate the rules.
- Phase 3: Validate that every operation is possible under their corresponding state. If not, remove that operation.
- Phase 4: Apply the operation at the state and compare the resulting state with the carried target state. If they do not match, modify or remove the transition. If the resulting state violates the rules, remove the transition.

Phase 1 only enumerates. Do not apply rule-based filtering in phase 1.
For Hanoi specifically, the move rules about top disks and larger-on-smaller are not phase-2 state filters in this encoding. Use them in phases 3 and 4.

## Phase 3 specific guidance

For phase 3, `operation_is_possible_under_state(state_code, operation_code)` must check all three conditions:
1. The disk's current position is not the same as the target position (source != target).
2. The disk is the top disk on its current stand. A disk is on top only if no smaller-numbered disk is on the same stand.
3. No smaller-numbered disk is already on the target stand. A larger disk cannot be placed on top of a smaller disk.

All three conditions must be satisfied for the operation to be possible.

## Phase 4 specific guidance

For phase 4, `transition_satisfies_rules(state_code, operation_code, next_state_code)` must verify the full set of Hanoi rules:
1. Source position != target position (the disk actually moves).
2. Only the moved disk's fact changed: `changed_items == [moved_disk]`.
3. The moved disk is the top disk on its source stand (no smaller disk on the same peg in the source state).
4. No smaller disk exists on the target stand (no larger-on-smaller violation in the source state).

For `apply_operation_under_state`, copy the state and update only the moved disk's position.

For `REPO_OPERATION_NAME_OVERRIDES`, there is one naming quirk:
- `(State_cbc, Op_2a)` must map to `move_yellow_hanoi_ring_to_the_center_of_blue_hanoi_stand` (not `brown`).

Use the scaffold exactly as provided for the current phase. Keep the helper names from the template unchanged.
Use the scaffold exactly as provided for the current phase:
- Phase 1: fill `base_position_codes`, `expand_state_codes`, `encode_state`, `build_candidate_operations`, `parse_operation`, and `target_state_for_operation`.
- Phase 2: fill `parse_state`, `state_satisfies_rules`, and `canonical_state_name`.
- Phase 3: fill `parse_state`, `parse_operation`, and `operation_is_possible_under_state`.
- Phase 4: fill `parse_state`, `encode_state`, `parse_operation`, `apply_operation_under_state`, `target_state_matches`, `transition_satisfies_rules`, and `repo_operation_name`.
Do not rename these helpers and do not invent a different phase API.

The final SMSL JSON will be produced by executing your phase scripts locally. The JSON template below is only a structural example for the shape that phase 4 must generate. Do not return that JSON in your response.

The shared scaffold for the current phase is below. Preserve its structure and helper function names, and fill in the missing constants and function bodies for this puzzle. Do not delete scaffold helpers, do not rename them, and do not leave placeholder names or `NotImplementedError` calls in your answer. Each phase script is executed as a standalone file, so do not reference constants, helpers, lookup tables, or parsing utilities unless they are defined in that same file.

# Structural Templates

%Reference SMSL JSON shape file%

{{SMSL_TEMPLATE}}

%Phase {{CURRENT_PHASE_NUMBER}} script template file%

{{CURRENT_PHASE_TEMPLATE}}
